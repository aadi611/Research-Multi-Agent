OPENAI_API_KEY=your_openai_api_key_here

# Optional: Redis (falls back to in-memory cache if not set)
REDIS_URL=redis://localhost:6379

# Optional: ChromaDB path (defaults to ./chroma_db)
CHROMA_DB_PATH=./chroma_db
